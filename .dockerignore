# Dependencies & build artifacts (rebuilt inside the image)
node_modules
.output
.vinxi
.nitro
.tanstack
dist
dist-ssr

# Local deployment artifacts (not needed in the image)
dist-deploy
New-Image
scripts/make-zips.ps1
deploy

# Secrets — never copy into the image
.env
.env.*
!.env.example

# Git & tooling
.git
.gitignore
.vscode
.idea
.DS_Store
*.log

# Source-only junk that isn't part of the website
public/uploads/*.docx
public/uploads/*.md
public/uploads/_docx_*.txt
public/uploads/screencapture-*
public/uploads/*.zip
